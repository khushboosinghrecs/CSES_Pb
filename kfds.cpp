#include <bits/stdc++.h>
using namespace std;
int main()

{
    int n;
    cin >> n;
    int arr[n];
    int sum = 0;

    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    int sum = 0;
    for (int i = 1; i < input1; i++)
    {
        sum = sum + (input2[i] - input2[0]) * 0.1;
    }
    return sum;
    return 0;
}